language: go

go:
  - "1.10"
  - "1.11"
  - "1.x"

os: ubuntu
dist: xenial

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sudo sh

install:
  - make install
